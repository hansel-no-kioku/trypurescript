{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "pulpunte build --skip-bundle",
    "bundle": "pulpunte run -m Setup && pulpunte build --to js/index.js"
  },
  "devDependencies": {
    "pulpunte": "^0.2.0",
    "purescript": "^0.13.5",
    "rimraf": "^2.5.4"
  }
}
